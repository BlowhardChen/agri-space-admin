import FeaturePlaceholder from "@/views/common/featurePlaceholder";

/** 渲染农服订单页面入口。 */
const FarmServiceOrder = () => {
	// 展示农服订单功能的迁移状态。
	return <FeaturePlaceholder title="农服订单" path="/mall/order/farmService" />;
};

export default FarmServiceOrder;
