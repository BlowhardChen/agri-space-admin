import FeaturePlaceholder from "@/views/common/featurePlaceholder";

/** 渲染审核管理页面入口。 */
const AuditManagement = () => {
	// 展示审核管理功能的迁移状态。
	return <FeaturePlaceholder title="审核管理" path="/audit" />;
};

export default AuditManagement;
