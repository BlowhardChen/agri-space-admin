import FeaturePlaceholder from "@/views/common/featurePlaceholder";

/** 渲染区域经理配置页面入口。 */
const RegionalManagerConfiguration = () => {
	// 展示区域经理配置功能的迁移状态。
	return <FeaturePlaceholder title="区域经理配置" path="/store/configure/manager" />;
};

export default RegionalManagerConfiguration;
